---
phase: 01-platform-foundation
plan: 01
subsystem: database
tags: [sqlite, stdlib, persistence]

requires: []
provides:
  - "backend/app/db package: init_database(), get_connection(), get_db_path(), DEFAULT_TICKERS, DEFAULT_CASH"
  - "Six-table SQLite schema (users_profile, watchlist, positions, trades, portfolio_snapshots, chat_messages) per PLAN.md §7"
  - "Idempotent lazy-init + default seed (one $10k profile, 10-ticker watchlist)"
affects: [01-platform-foundation, portfolio, trading, chat]

tech-stack:
  added: []
  patterns:
    - "Standalone db package, no ORM, stdlib sqlite3 only"
    - "CREATE TABLE IF NOT EXISTS + INSERT OR IGNORE for idempotent lazy init"
    - "Env-driven path resolution (FINALLY_DB_PATH) with a hardcoded default"

key-files:
  created:
    - backend/app/db/__init__.py
    - backend/app/db/schema.py
    - backend/app/db/connection.py
    - backend/app/db/seed.py
    - backend/tests/db/__init__.py
    - backend/tests/db/test_database.py
  modified: []

key-decisions:
  - "DEFAULT_TICKERS defined explicitly in app/db/seed.py (not imported from app.market.seed_prices) so the db package has zero dependency on the market subsystem, matching PLAN.md's directory-boundary guidance."
  - "seed_defaults() takes an open sqlite3.Connection (not a path) so init_database can create schema and seed within a single connection lifecycle."
  - "Timestamps are always ISO strings generated by Python (datetime.now(timezone.utc).isoformat()), never SQL defaults, per plan guidance."

requirements-completed: [DB-01, DB-02, DB-03]

coverage:
  - id: D1
    description: "init_database() creates db/finally.db (and parent dirs) with all six tables when the file is absent"
    requirement: "DB-01"
    verification:
      - kind: unit
        ref: "backend/tests/db/test_database.py#TestDatabase::test_init_creates_file"
        status: pass
      - kind: unit
        ref: "backend/tests/db/test_database.py#TestDatabase::test_all_six_tables_present"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every table carries a user_id column (default 'default'); watchlist/positions enforce UNIQUE(user_id, ticker); schema matches PLAN.md §7 exactly"
    requirement: "DB-02"
    verification:
      - kind: unit
        ref: "backend/tests/db/test_database.py#TestDatabase::test_expected_columns"
        status: pass
    human_judgment: false
  - id: D3
    description: "Seeding yields exactly one $10,000 users_profile row and 10 default watchlist tickers (AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX)"
    requirement: "DB-03"
    verification:
      - kind: unit
        ref: "backend/tests/db/test_database.py#TestDatabase::test_seed_profile_and_watchlist"
        status: pass
    human_judgment: false
  - id: D4
    description: "init_database() is idempotent: calling it twice keeps counts stable (1 profile, 10 tickers) and raises no error"
    requirement: "DB-01"
    verification:
      - kind: unit
        ref: "backend/tests/db/test_database.py#TestDatabase::test_idempotent_reinit"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-07-04
status: complete
---

# Phase 1 Plan 1: SQLite Database Layer Summary

**Standalone stdlib-sqlite3 persistence package (six-table schema, env-driven connection, idempotent lazy init + seed) with zero dependency on the market subsystem, ready for Plan 01-02 to wire into FastAPI's request lifecycle.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-04T22:03:00Z
- **Completed:** 2026-07-04T22:15:56Z
- **Tasks:** 3
- **Files modified:** 6 (all created; none pre-existing)

## Accomplishments
- `backend/app/db/schema.py`: `SCHEMA_STATEMENTS` (six `CREATE TABLE IF NOT EXISTS` statements) and `TABLE_NAMES`, transcribed exactly from planning/PLAN.md §7.
- `backend/app/db/connection.py`: `get_db_path()` (FINALLY_DB_PATH env override, default `db/finally.db`), `get_connection()` (creates parent dir, Row-factory connection), and `init_database()` (schema + seed, idempotent, logs on first creation only).
- `backend/app/db/seed.py`: `DEFAULT_TICKERS`, `DEFAULT_CASH = 10000.0`, and `seed_defaults()` using `INSERT OR IGNORE` for both the profile and each watchlist row, keyed on the `id='default'` PK and the `UNIQUE(user_id, ticker)` constraint respectively.
- `backend/tests/db/test_database.py`: 5-test `TestDatabase` class covering file creation, all-six-tables presence, expected columns + UNIQUE constraint enforcement, seed correctness, and re-init idempotency.

## Task Commits

Each task was committed atomically:

1. **Task 1: Schema DDL + env-driven connection helper** - `a691f8e` (feat)
2. **Task 2: Default seed + idempotent init_database()** - `038d071` (feat)
3. **Task 3: Pytest suite for schema, seed, and idempotency** - `102133b` (test)

**Plan metadata:** _pending — recorded after final docs commit_

## Files Created/Modified
- `backend/app/db/schema.py` - Six-table DDL (`SCHEMA_STATEMENTS`) + `TABLE_NAMES`
- `backend/app/db/connection.py` - `get_db_path`, `get_connection`, `init_database`
- `backend/app/db/seed.py` - `DEFAULT_TICKERS`, `DEFAULT_CASH`, `seed_defaults`
- `backend/app/db/__init__.py` - Public API re-exports + module docstring
- `backend/tests/db/__init__.py` - Test package marker
- `backend/tests/db/test_database.py` - 5 tests: creation, tables, columns/constraints, seed, idempotency

## Decisions Made
- Kept `app/db` fully independent of `app/market` (no import of `SEED_PRICES` keys) per the plan's explicit directory-boundary note, even though the 10 tickers are duplicated as literals in both modules. Future refactor could hoist a single shared ticker-list constant if drift becomes a concern, but that's out of scope for this plan.
- Used `INSERT OR IGNORE` (rather than manual `SELECT` existence checks) for both idempotency paths — simpler, atomic, and relies on the PK/UNIQUE constraints already required by the schema.

## Deviations from Plan

None — plan executed exactly as written. Tasks 1 and 2 (`tdd="true"`) used the plan's own inline Python one-liner `<verify>` commands as the RED→GREEN gate rather than separate pytest files, per the plan's explicit design of deferring the formal pytest suite to Task 3. See "TDD Gate Compliance" below.

## TDD Gate Compliance

This plan's Tasks 1 and 2 are marked `tdd="true"` but their `<action>` blocks direct implementation directly into `schema.py`/`connection.py`/`seed.py` with plan-authored inline `<verify>` one-liners (not standalone test files) — the plan intentionally reserves the formal `test(...)` pytest suite for Task 3, executed after the implementation. This means the git history for this plan is `feat` → `feat` → `test`, not the canonical `test` → `feat` → `refactor` RED/GREEN/REFACTOR sequence.

- Each of Tasks 1 and 2's inline verify commands was run and failed appropriately when checked against fresh code, then passed after implementation (functionally satisfying RED→GREEN at the granularity the plan specified).
- Task 3 then added the full behavioral pytest suite (5 tests, all passing) as `test(01-01): ...` — this is the canonical "test" commit for the plan but arrives last, not first, in the git log.

No functional gap results — coverage is complete (see `coverage:` block above) — but this is flagged per the executor's gate-sequence-validation instruction since a strict `test` before `feat` ordering is absent for this plan.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. Stdlib `sqlite3` only, no new dependency.

## Next Phase Readiness
- `app.db.init_database`, `get_connection`, `get_db_path`, `DEFAULT_TICKERS`, `DEFAULT_CASH` are ready for Plan 01-02 to wire into FastAPI's "create on first request" middleware/lifespan.
- No HTTP surface exists yet (by design) — this plan is purely the data engine.
- No blockers.

---
*Phase: 01-platform-foundation*
*Completed: 2026-07-04*

## Self-Check: PASSED

All 6 created files confirmed present on disk; all 3 task commit hashes (a691f8e, 038d071, 102133b) confirmed in git log.
