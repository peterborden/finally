---
phase: 04-frontend-terminal-ui
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/components/ConnectionDot.tsx
  - frontend/components/Header.tsx
  - frontend/components/PositionsTable.tsx
  - frontend/components/TradeBar.tsx
autonomous: true
requirements: [UI-06, UI-07, UI-09]
must_haves:
  truths:
    - "The header shows live total portfolio value, cash balance, and a connection-status dot (green connected / yellow reconnecting / red disconnected)"
    - "The positions table lists ticker, quantity, avg cost, current price, unrealized P&L, and % change for every held position"
    - "The trade bar fires an instant market order (ticker, quantity, Buy/Sell) with no confirmation dialog and surfaces backend errors inline"
  artifacts:
    - frontend/components/Header.tsx
    - frontend/components/ConnectionDot.tsx
    - frontend/components/PositionsTable.tsx
    - frontend/components/TradeBar.tsx
  key_links:
    - "TradeBar calls api.trade then invokes onTraded() so the parent re-fetches /api/portfolio (04-06 wiring)"
    - "Header total value updates live from cache prices; ConnectionDot driven by useLivePrices status"
---

<objective>
Build the header (live total value, cash balance, connection-status dot — UI-09), the positions table (ticker, quantity, avg cost, current price, unrealized P&L, % change — UI-06), and the trade bar that fires instant market orders with no confirmation dialog (UI-07). These are the DOM/table/input surfaces of the terminal.

Purpose: Portfolio state visibility and the manual trading entry point — the core "trade instantly" capability.

Output: `Header.tsx`, `ConnectionDot.tsx`, `PositionsTable.tsx`, `TradeBar.tsx` consumed by the page assembly in 04-06.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-terminal-ui/04-CONTEXT.md
@frontend/lib/types.ts
@frontend/lib/api.ts
@frontend/lib/useLivePrices.ts
# Portfolio/Trade response shapes and field semantics (pct_change is a fraction):
@backend/app/portfolio.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConnectionDot + Header (live total value, cash, status dot)</name>
  <read_first>
    - frontend/lib/types.ts — Portfolio {cash_balance, total_value, positions[...] , total_unrealized_pnl}; ConnectionStatus.
    - frontend/lib/useLivePrices.ts — status is 'connecting' | 'connected' | 'reconnecting'.
    - 04-CONTEXT.md Layout "Header" (app name, live total value, cash, connection dot green/yellow/red) and Data & State ("Total value in the header updates live from cache prices × positions").
  </read_first>
  <files>frontend/components/ConnectionDot.tsx, frontend/components/Header.tsx</files>
  <action>
    ConnectionDot.tsx: a `'use client'` component taking `status: ConnectionStatus`. Render a small colored dot + label: green (up token) for 'connected', yellow (accent token) for 'reconnecting'/'connecting', red (down token) for a disconnected state. Include an accessible title/aria-label.
    Header.tsx: a `'use client'` top bar. Props: `portfolio: Portfolio | null`, `prices: PriceMap`, `status: ConnectionStatus`. Left: "FinAlly" wordmark in the accent color. Right: live total value and cash balance in tabular/monospace, plus `<ConnectionDot>`. Compute a LIVE total value from cash + sum(position.quantity × live price from `prices[ticker]?.price`, falling back to `position.current_price` then `avg_cost`) so the header ticks with the SSE stream rather than only on portfolio re-fetch. Format currency to 2 decimals. When portfolio is null, show placeholders.
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Header shows wordmark + live total value + cash + ConnectionDot; the dot color maps connected→green, reconnecting/connecting→yellow, disconnected→red; total value recomputes from live prices.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - ConnectionDot maps the three statuses to green/yellow/red tokens.
    - Header total value uses live `prices` (not only the fetched snapshot).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PositionsTable + TradeBar (instant market orders)</name>
  <read_first>
    - backend/app/portfolio.py — Position fields {ticker, quantity, avg_cost, current_price, unrealized_pnl, pct_change}; pct_change is a FRACTION (multiply by 100 for display). TradeRequest {ticker, side: "buy"|"sell", quantity>0}. Errors return HTTP 400 with a `detail` string (e.g. insufficient cash/shares).
    - frontend/lib/api.ts — trade({ticker, side, quantity}) throws Error(detail) on non-ok.
    - 04-CONTEXT.md Layout "Bottom band" (positions table columns; trade bar: ticker field, quantity field, Buy + Sell buttons — instant market order, no confirmation dialog). Purple #753991 is the submit/buy-confirm button color.
  </read_first>
  <files>frontend/components/PositionsTable.tsx, frontend/components/TradeBar.tsx</files>
  <action>
    PositionsTable.tsx: a `'use client'` table. Props: `positions: Position[]`, `prices: PriceMap`. Columns: Ticker, Quantity, Avg Cost, Current Price, Unrealized P&L, % Change. Prefer the live `prices[ticker]?.price` for Current Price when available (else position.current_price); recompute unrealized P&L = (livePrice − avg_cost) × quantity and % change = livePrice/avg_cost − 1 for live rows so the table tracks the stream. Color P&L / % change green (up token) when ≥ 0, red (down token) when < 0. Render `pct_change × 100` with a % suffix. Tabular/monospace numerals. Empty state when no positions.
    TradeBar.tsx: a `'use client'` form. Props: `onTraded(): void` (parent re-fetches portfolio/watchlist). Fields: ticker text input, quantity number input (>0), a Buy button and a Sell button. On Buy/Sell click, call `api.trade({ticker, side, quantity})` immediately — NO confirmation dialog (deliberate per spec). On success, clear the quantity, show a brief inline success note (filled qty @ price from the response), and call `onTraded()`. On failure, show the thrown Error `detail` inline (e.g. "insufficient cash") without clearing inputs. Disable buttons while a request is in flight. Style the submit buttons with the purple token per the locked scheme.
    Do NOT render any confirmation modal/dialog — the acceptance test greps against it.
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>PositionsTable shows all six columns with live-updated current price / P&L colored by sign; TradeBar Buy/Sell call api.trade with no confirmation step, surface errors inline, and call onTraded on success.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - TradeBar has Buy and Sell buttons that call api.trade directly (no confirm()/modal gating the request).
    - PositionsTable renders pct_change as a percentage (fraction × 100) and colors P&L by sign.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → same-origin /api/portfolio/trade | Trade ticker/quantity/side crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | TradeBar inputs | low | mitigate | Backend validates side (buy/sell), quantity>0, cash/shares; client passes values through and renders the backend's `detail` error inline. Values rendered as React text nodes (auto-escaped) |
| T-04-06 | Repudiation | instant no-confirm trade | low | accept | No confirmation dialog is an explicit design decision (simulated money); trades are logged server-side in the trades table |
</threat_model>

<verification>
- `npm --prefix frontend run typecheck` exits 0.
- Components compile in the 04-06 full build.
</verification>

<success_criteria>
- Header reflects live total value, cash, and connection status.
- Positions table shows the six required columns, live-updated and sign-colored.
- Trade bar fires instant market orders with inline error surfacing and no confirmation dialog.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-terminal-ui/04-03-SUMMARY.md` when done.
</output>
