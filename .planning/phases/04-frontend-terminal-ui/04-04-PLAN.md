---
phase: 04-frontend-terminal-ui
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/components/PortfolioHeatmap.tsx
  - frontend/components/PnLChart.tsx
autonomous: true
requirements: [UI-04, UI-05]
must_haves:
  truths:
    - "The portfolio heatmap/treemap renders one rectangle per position, sized by portfolio weight and colored by P&L (green profit ↔ red loss)"
    - "The P&L line chart plots total portfolio value over time from GET /api/portfolio/history"
  artifacts:
    - frontend/components/PortfolioHeatmap.tsx
    - frontend/components/PnLChart.tsx
  key_links:
    - "PortfolioHeatmap sizes rectangles by position weight = (quantity × current price) / total position value; color by unrealized_pnl sign/magnitude"
    - "PnLChart consumes Snapshot[] from api.getHistory (total_value over recorded_at)"
---

<objective>
Build the two portfolio charts: the recharts Treemap heatmap (rectangle per position, sized by weight, colored by P&L — UI-04) and the lightweight-charts P&L line chart of total portfolio value over time from `/api/portfolio/history` (UI-05).

Purpose: The portfolio-visualization surfaces that make the terminal feel like a trading workstation.

Output: `PortfolioHeatmap.tsx`, `PnLChart.tsx` consumed by the page assembly in 04-06.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-terminal-ui/04-CONTEXT.md
@frontend/lib/types.ts
@frontend/lib/api.ts
@frontend/lib/useLivePrices.ts
# Position fields + history snapshot shape:
@backend/app/portfolio.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PortfolioHeatmap — recharts Treemap sized by weight, colored by P&L</name>
  <read_first>
    - frontend/lib/types.ts — Position {ticker, quantity, avg_cost, current_price, unrealized_pnl, pct_change}.
    - 04-CONTEXT.md Layout "Bottom band" (heatmap: rectangle per position, sized by weight, colored by P&L green↔red) and Stack (recharts Treemap for the heatmap).
    - Consult Context7 for the installed recharts v2 Treemap API (data shape, `dataKey`, custom `content` renderer).
  </read_first>
  <files>frontend/components/PortfolioHeatmap.tsx</files>
  <action>
    PortfolioHeatmap.tsx: a `'use client'` component. Props: `positions: Position[]`, `prices: PriceMap`. Build treemap data: one node per position with `name: ticker`, `size` = position weight proxy = quantity × live price (`prices[ticker]?.price` else current_price), and carry `pnl` (recomputed unrealized P&L from live price when available) plus pct for coloring. Render a recharts `<Treemap>` inside a `<ResponsiveContainer>` with `dataKey="size"`, `aspectRatio` reasonable for the bottom band. Provide a custom `content` renderer that colors each cell by P&L: green (up token) for profit, red (down token) for loss, with intensity/opacity scaled by magnitude (e.g. |pct| clamped); draw the ticker label and P&L inside the rect when it is large enough. Empty state ("No positions") when the list is empty. Use only the locked palette. Because recharts touches the DOM, keep this client-only ('use client' already set).
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>PortfolioHeatmap renders a recharts Treemap with one rectangle per position sized by weight and colored green/red by P&L, with an empty state.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - Treemap `dataKey` is the per-position weight; cell color derives from P&L sign.
    - Renders an empty state with zero positions.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PnLChart — lightweight-charts line of total value over time</name>
  <read_first>
    - backend/app/portfolio.py — GET /api/portfolio/history returns `[{id, total_value, recorded_at}]` ordered by recorded_at (recorded_at is an ISO-8601 UTC string).
    - frontend/lib/api.ts — getHistory() returns Snapshot[].
    - 04-CONTEXT.md Stack (lightweight-charts for the P&L line chart) and Data & State (fetch /api/portfolio/history on load; re-fetch after trades).
    - Consult Context7 for lightweight-charts v5 API (createChart, addSeries(LineSeries|AreaSeries, ...)).
  </read_first>
  <files>frontend/components/PnLChart.tsx</files>
  <action>
    PnLChart.tsx: a `'use client'` component. Props: `history: Snapshot[]`. In an effect, create a lightweight-charts chart in a container ref with dark-theme options matching the palette (accent or blue line). Map snapshots to series data `{ time, value: total_value }` where time is derived from `recorded_at` (parse the ISO string to a UTC timestamp in seconds; ensure strictly ascending — lightweight-charts requires ordered, unique times, so de-duplicate/coerce equal seconds by nudging). Use the v5 `chart.addSeries(LineSeries, ...)` (or AreaSeries for a filled look) — NOT v4's removed `addLineSeries`. Update the series when `history` changes. Handle resize and dispose on unmount. Empty state when history is empty (before the first snapshot).
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>PnLChart renders a lightweight-charts line of total_value over recorded_at from the history prop, updates when history changes, and disposes on unmount.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - Times are parsed from ISO recorded_at into ascending unique series times.
    - Uses v5 `addSeries(...)` and disposes the chart on unmount.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → same-origin /api/portfolio/history | Portfolio snapshot history crosses here (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Denial of Service | PnLChart series growth | low | accept | History is server-bounded (snapshots every ~30s + per-trade); chart handles the full ordered series, redraw cost is negligible for a single session |
| T-04-08 | Tampering | Treemap label rendering | low | mitigate | Ticker labels rendered via SVG text nodes from typed data, no HTML injection |
</threat_model>

<verification>
- `npm --prefix frontend run typecheck` exits 0.
- Components compile in the 04-06 full build.
</verification>

<success_criteria>
- Heatmap treemap sizes by weight and colors by P&L.
- P&L line chart plots total portfolio value over time from the history endpoint.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-terminal-ui/04-04-SUMMARY.md` when done.
</output>
