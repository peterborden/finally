---
phase: 04-frontend-terminal-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/next.config.mjs
  - frontend/tsconfig.json
  - frontend/postcss.config.mjs
  - frontend/tailwind.config.ts
  - frontend/.gitignore
  - frontend/next-env.d.ts
  - frontend/app/layout.tsx
  - frontend/app/globals.css
  - frontend/app/page.tsx
  - frontend/lib/types.ts
  - frontend/lib/api.ts
  - frontend/lib/useLivePrices.ts
autonomous: true
requirements: [UI-01, UI-10]
must_haves:
  truths:
    - "npm run build produces a static export at frontend/out with an index.html"
    - "The exported page renders on the #0d1117 dark theme with the locked accent colors available as Tailwind tokens"
    - "useLivePrices opens EventSource('/api/stream/prices'), parses the ticker-keyed JSON, and exposes a live price map, per-ticker history, and a connection status"
  artifacts:
    - frontend/next.config.mjs
    - frontend/tailwind.config.ts
    - frontend/app/globals.css
    - frontend/lib/types.ts
    - frontend/lib/api.ts
    - frontend/lib/useLivePrices.ts
    - frontend/out/index.html
  key_links:
    - "next.config.mjs output:'export' + images.unoptimized => frontend/out is generated (FastAPI FRONTEND_DIST target)"
    - "useLivePrices SSE parse matches backend stream.py payload shape {TICKER: {ticker,price,previous_price,timestamp,change,change_percent,direction}}"
---

<objective>
Scaffold a self-contained Next.js (app-router) + TypeScript static-export project in `frontend/`, install all runtime dependencies, wire the locked dark Tailwind theme, and build the shared foundation every other Phase 4 plan consumes: TypeScript types matching the backend, a relative `/api/*` fetch client, and the `useLivePrices` SSE hook. (UI-01 theme/layout foundation, UI-10 native EventSource with auto-reconnect.)

Purpose: Every downstream component depends on the build pipeline, the theme tokens, the API client, and the live-price hook. This plan makes `npm run build` produce `frontend/out` (the directory FastAPI serves via `FRONTEND_DIST`) and gives the SSE contract a single, correct implementation.

Output: A buildable Next.js static-export project with theme, types, API client, SSE hook, and a placeholder terminal shell page.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-terminal-ui/04-CONTEXT.md

# Exact SSE payload shape (object keyed by ticker) and per-ticker fields:
@backend/app/market/stream.py
@backend/app/market/models.py
# Backend API response shapes the client types must mirror:
@backend/app/portfolio.py
@backend/app/watchlist.py
@backend/app/chat.py
# FRONTEND_DIST resolution (frontend/out is the served directory):
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Next.js static-export project + install dependencies</name>
  <read_first>
    - backend/app/main.py `_resolve_frontend_dir` — the build output `frontend/out` is what FRONTEND_DIST points at.
    - 04-CONTEXT.md "Stack" and "Color & Theme" — locked stack and exact hex tokens.
  </read_first>
  <files>frontend/package.json, frontend/package-lock.json, frontend/next.config.mjs, frontend/tsconfig.json, frontend/postcss.config.mjs, frontend/tailwind.config.ts, frontend/.gitignore, frontend/next-env.d.ts</files>
  <action>
    Create a Next.js app-router TypeScript project by hand in `frontend/` (do NOT run create-next-app interactively; write the config files directly, then run npm install). Pin deterministic majors: `next@^15`, `react@^19`, `react-dom@^19`, `typescript@^5`, `@types/react`, `@types/react-dom`, `@types/node`, and Tailwind v3 (`tailwindcss@^3`, `postcss`, `autoprefixer`) — Tailwind v3 is required for the config-file theme approach below; do NOT use Tailwind v4. Also install the charting deps now so no later plan touches package.json: `lightweight-charts@^5` and `recharts@^2`.
    package.json scripts: `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`, `"typecheck": "tsc --noEmit"`, `"lint": "next lint"`.
    next.config.mjs: export a config with `output: 'export'` and `images: { unoptimized: true }` so `next build` emits a static site to `frontend/out` (per D — locked stack). Do not set `basePath`.
    tsconfig.json: standard Next.js app-router TS config, `"strict": true`, `moduleResolution": "bundler"`, path alias `"@/*": ["./*"]`, include `next-env.d.ts` and `**/*.ts(x)`.
    postcss.config.mjs: plugins tailwindcss + autoprefixer.
    tailwind.config.ts: `content` globs `./app/**/*.{ts,tsx}` and `./components/**/*.{ts,tsx}`; `darkMode: 'class'`; extend `theme.colors` with the EXACT locked tokens (per 04-CONTEXT.md Color & Theme): base `#0d1117`, panel `#1a1a2e`, `border-muted` `#2a2a3a`, accent `#ecad0a`, blue `#209dd7`, purple `#753991`, up `#3fb950`, down `#f85149`. Add a `fontFamily.mono` stack for tabular price numerals.
    .gitignore: ignore `node_modules/`, `.next/`, `out/`, `next-env.d.ts` is committed per Next convention (do NOT ignore it).
    Run `npm --prefix frontend install` to generate package-lock.json and node_modules. Package installs are pre-approved for this phase (standard first-party packages — see threat_model T-04-SC).
  </action>
  <verify>
    <automated>npm --prefix frontend install --no-audit --no-fund && test -f frontend/package-lock.json && node -e "const p=require('./frontend/package.json');['next','react','tailwindcss','lightweight-charts','recharts'].forEach(d=>{if(!(p.dependencies?.[d]||p.devDependencies?.[d]))throw new Error('missing '+d)})"</automated>
  </verify>
  <done>package.json lists next/react/tailwindcss/lightweight-charts/recharts; package-lock.json exists; next.config.mjs sets output:'export' and images.unoptimized; tailwind.config.ts contains all eight locked hex tokens.</done>
  <acceptance_criteria>
    - `npm --prefix frontend install` completes and produces package-lock.json.
    - next.config.mjs contains `output: 'export'` and `images: { unoptimized: true }`.
    - tailwind.config.ts defines colors base, panel, accent (#ecad0a), blue (#209dd7), purple (#753991), up, down.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Theme shell — layout, globals.css (flash animation), placeholder page</name>
  <read_first>
    - 04-CONTEXT.md "Color & Theme" (price flash fades ~500ms via CSS transition) and "Specific Ideas" (add a CSS class on change, remove after transition).
  </read_first>
  <files>frontend/app/layout.tsx, frontend/app/globals.css, frontend/app/page.tsx</files>
  <action>
    app/layout.tsx: root layout exporting `metadata` (title "FinAlly") and a `<html lang="en" className="dark">` / `<body>` that imports `./globals.css` and applies the base background/foreground. Body classes: `bg-base text-gray-200 font-mono antialiased`.
    app/globals.css: `@tailwind base; @tailwind components; @tailwind utilities;` then base styles (html/body height 100%, background base color, never pure black). Define price-flash utility classes: `.flash-up` and `.flash-down` apply a brief background tint (green/red) that transitions back to transparent over ~500ms (use `transition: background-color 500ms ease-out`), so a component can add the class on a price change and remove it after the transition. Also add a `.tabular` helper using `font-variant-numeric: tabular-nums` for aligned price columns.
    app/page.tsx: a minimal `'use client'` placeholder terminal shell that renders a full-height `<main class="min-h-screen bg-base">` with a header bar reading "FinAlly" in the accent color and a note that panels mount here. This is intentionally a stub — plan 04-06 replaces it with the full Bloomberg layout. Do NOT import components that other wave-2 plans create (they do not exist yet).
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>tsc --noEmit passes; globals.css defines .flash-up/.flash-down with a ~500ms background-color transition; page.tsx renders a dark shell using the accent token.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - globals.css contains `.flash-up`, `.flash-down`, and a 500ms background-color transition.
    - page.tsx compiles standalone (imports no not-yet-created components).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared types, /api client, and the useLivePrices SSE hook</name>
  <read_first>
    - backend/app/market/models.py `PriceUpdate.to_dict` — fields: ticker, price, previous_price, timestamp, change, change_percent, direction.
    - backend/app/market/stream.py — the SSE `data:` payload is a JSON OBJECT keyed by ticker: `{"AAPL": {..fields..}, "GOOGL": {...}}` (NOT one ticker per event). A leading `retry: 1000` line is sent.
    - backend/app/portfolio.py — PortfolioResponse {cash_balance, positions[{ticker,quantity,avg_cost,current_price,unrealized_pnl,pct_change}], total_value, total_unrealized_pnl}; TradeResponse {ticker,side,filled_quantity,filled_price,portfolio}; SnapshotResponse {id,total_value,recorded_at}. Note pct_change is a fraction (0.05 = 5%).
    - backend/app/watchlist.py — WatchlistEntry {ticker, price?, previous_price?, change?, change_percent?, direction?, added_at?}. change_percent is already a percentage (0.5 = 0.5%).
    - backend/app/chat.py — ChatResponseBody {message, actions[{type,ticker,status,detail,side?,action?,quantity?,filled_price?}]}.
  </read_first>
  <files>frontend/lib/types.ts, frontend/lib/api.ts, frontend/lib/useLivePrices.ts, frontend/lib/useLivePrices.test.ts, frontend/package.json</files>
  <behavior>
    - parseSseData(rawJsonObject) → returns an array/record of PriceEvent with all seven fields typed correctly.
    - useLivePrices maintains a `prices: Record<ticker, PriceEvent>` merged on each event, and `history: Record<ticker, {t:number, price:number}[]>` appending one point per ticker per event, capped at a max length (e.g. 300) to bound memory.
    - Connection status: 'connecting' initially, 'connected' after EventSource onopen, 'reconnecting' after onerror (EventSource retries automatically — do NOT manually close+reopen on error).
    - The hook closes the EventSource on unmount.
  </behavior>
  <action>
    lib/types.ts: export interfaces mirroring the backend exactly — `PriceEvent`, `PriceMap = Record<string, PriceEvent>`, `WatchlistEntry`, `Position`, `Portfolio`, `TradeResponse`, `Snapshot`, `ChatAction`, `ChatResponse`, and a `ConnectionStatus = 'connecting' | 'connected' | 'reconnecting'`. Use optional (`?`) fields where the backend returns None (WatchlistEntry price fields). Document with a comment that `Position.pct_change` is a fraction and `PriceEvent.change_percent`/`WatchlistEntry.change_percent` are already percentages.
    lib/api.ts: thin fetch wrappers to relative paths (same-origin, no CORS): `getWatchlist()`, `addTicker(ticker)`, `removeTicker(ticker)`, `getPortfolio()`, `trade({ticker, side, quantity})`, `getHistory()`, `sendChat(message)`. Each checks `res.ok`; on non-ok, read the JSON `detail` field (FastAPI HTTPException shape) and throw an Error carrying that detail so callers can surface e.g. "insufficient cash" inline.
    lib/useLivePrices.ts: a `'use client'` React hook implementing the behavior above. On mount create `new EventSource('/api/stream/prices')`; in `onmessage` JSON.parse the data (the ticker-keyed object), merge into the prices map, and append `{t: Date.now(), price}` to each ticker's history (cap length). Set status on `onopen`/`onerror`. Return `{ prices, history, status }`. Clean up in the effect's return.
    Add a colocated unit test `lib/useLivePrices.test.ts` covering the pure parse/merge/cap logic — extract the merge+history-cap into an exported pure helper (e.g. `applyPriceEvent(state, payload, cap)`) so it is testable without a DOM. Wire a minimal test runner: add `vitest` as a devDependency in this task's npm install and a `"test": "vitest run"` script.
    Note for executor: consult Context7 for lightweight-charts v5 / recharts APIs in later plans; this plan needs no chart code.
  </action>
  <verify>
    <automated>npm --prefix frontend install --no-audit --no-fund && npm --prefix frontend run typecheck && npm --prefix frontend test</automated>
  </verify>
  <done>types.ts mirrors all backend shapes; api.ts throws with backend `detail` on non-ok; useLivePrices parses the ticker-keyed SSE object, caps history, and reports connection status; the applyPriceEvent unit test passes.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - `npm --prefix frontend test` runs the applyPriceEvent test and passes (merges multiple tickers, caps history length).
    - api.ts uses only relative `/api/*` URLs.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → same-origin /api/* | All data (prices, portfolio, chat) crosses here; no auth by design (single-user) |
| npm registry → build | Third-party packages enter the build/supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | api.ts error surfacing | low | mitigate | Surface only backend `detail` strings; never log secrets (none exist client-side) |
| T-04-02 | Denial of Service | useLivePrices history buffer | medium | mitigate | Cap per-ticker history length (~300) to bound memory over a long-lived session |
| T-04-SC | Tampering | npm installs (next/react/tailwind/lightweight-charts/recharts/vitest) | high | accept | Packages are the canonical, widely-used first-party libraries explicitly pre-approved for this phase by the developer; versions pinned via package-lock.json. No [ASSUMED]/[SUS] packages introduced |
</threat_model>

<verification>
- `npm --prefix frontend install` succeeds and writes package-lock.json.
- `npm --prefix frontend run build` produces `frontend/out/index.html` (static export).
- `npm --prefix frontend run typecheck` and `npm --prefix frontend test` pass.
</verification>

<success_criteria>
- A buildable Next.js static-export project exists in `frontend/`.
- The locked dark theme tokens are available as Tailwind classes.
- types.ts, api.ts, and useLivePrices.ts provide the correct, tested foundation for all wave-2 components.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-terminal-ui/04-01-SUMMARY.md` when done.
</output>
