---
phase: 04-frontend-terminal-ui
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05"]
files_modified:
  - frontend/app/page.tsx
autonomous: false
requirements: [UI-01]
must_haves:
  truths:
    - "A single dark, data-dense Bloomberg-style page assembles the header, watchlist, main chart, portfolio heatmap, P&L chart, positions table, trade bar, and chat panel with the locked color scheme"
    - "Selecting a watchlist ticker updates the main PriceChart; trades and chat actions re-fetch /api/portfolio and /api/watchlist so the terminal updates instantly"
    - "npm run build produces frontend/out and, served via FRONTEND_DIST, the backend returns the terminal at /"
  artifacts:
    - frontend/app/page.tsx
    - frontend/out/index.html
  key_links:
    - "page.tsx owns the shared state: useLivePrices (prices/history/status), selectedTicker, portfolio, watchlist, history — and the refetch callbacks passed to Watchlist/TradeBar/ChatPanel"
    - "frontend/out is the FRONTEND_DIST directory FastAPI serves (backend/app/main.py)"
---

<objective>
Assemble the full single-page Bloomberg-style terminal in `app/page.tsx` (UI-01): mount `useLivePrices`, fetch watchlist/portfolio/history on load, hold `selectedTicker` state, lay out all eight components in the locked dark theme, and wire the re-fetch callbacks so trades (manual and chat-driven) and watchlist changes update the terminal instantly. Deliver a green `npm run build` producing `frontend/out`, and verify end-to-end that FastAPI serves the app when `FRONTEND_DIST=frontend/out`.

Purpose: This is the integration that turns the parallel wave-2 components into one working terminal — and the phase's observable deliverable.

Output: A complete `app/page.tsx` and a verified static export at `frontend/out`.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-terminal-ui/04-CONTEXT.md
@frontend/lib/types.ts
@frontend/lib/api.ts
@frontend/lib/useLivePrices.ts
@frontend/components/Header.tsx
@frontend/components/Watchlist.tsx
@frontend/components/PriceChart.tsx
@frontend/components/PositionsTable.tsx
@frontend/components/PortfolioHeatmap.tsx
@frontend/components/PnLChart.tsx
@frontend/components/TradeBar.tsx
@frontend/components/ChatPanel.tsx
# Served-directory contract:
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the terminal in page.tsx and wire shared state + refetch</name>
  <read_first>
    - All eight component prop contracts (Header, Watchlist, PriceChart, PositionsTable, PortfolioHeatmap, PnLChart, TradeBar, ChatPanel) from 04-02..04-05.
    - frontend/lib/useLivePrices.ts — returns {prices, history, status}.
    - frontend/lib/api.ts — getWatchlist, getPortfolio, getHistory.
    - 04-CONTEXT.md Layout (header top bar; left watchlist; center main chart; right collapsible chat; bottom band = heatmap + P&L chart + positions table + trade bar) and Data & State (on load fetch watchlist/portfolio/history + open SSE; after any trade or watchlist change re-fetch portfolio and watchlist).
  </read_first>
  <files>frontend/app/page.tsx</files>
  <action>
    Rewrite app/page.tsx (replacing the 04-01 placeholder) as the `'use client'` terminal root. State: `const { prices, history, status } = useLivePrices()`; `selectedTicker` (default to the first watchlist ticker once loaded); `watchlist: WatchlistEntry[]`; `portfolio: Portfolio | null`; `pnlHistory: Snapshot[]`. On mount (effect) call `api.getWatchlist()`, `api.getPortfolio()`, `api.getHistory()` and store results. Define `refreshPortfolio()` (re-fetch portfolio + history) and `refreshWatchlist()` (re-fetch watchlist); a combined `refreshAll()` for chat actions. Lay out with the locked palette: Header (portfolio, prices, status) as the top bar; a main grid — left column `<Watchlist entries prices history selected onSelect={setSelectedTicker} onWatchlistChange={refreshWatchlist} />`, center `<PriceChart ticker={selectedTicker} points={history[selectedTicker] ?? []} />`, right `<ChatPanel onActions={refreshAll} />` (collapsible); bottom band `<PortfolioHeatmap positions prices />`, `<PnLChart history={pnlHistory} />`, `<PositionsTable positions prices />`, and `<TradeBar onTraded={refreshAll} />`. Ensure the layout is dense, desktop-first, full-viewport-height, on the base background with panel-colored cards and muted borders. Pass live `prices` into Header/Watchlist/PositionsTable/PortfolioHeatmap so everything ticks with the SSE stream. Keep the whole tree client-side (charts require the browser); do not add server-component data fetching.
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck && npm --prefix frontend run build && test -f frontend/out/index.html</automated>
  </verify>
  <done>page.tsx mounts all eight components with correct props, fetches watchlist/portfolio/history on load, wires refreshWatchlist/refreshPortfolio/refreshAll into Watchlist/TradeBar/ChatPanel, and `npm run build` emits frontend/out/index.html.</done>
  <acceptance_criteria>
    - `npm --prefix frontend run typecheck` exits 0.
    - `npm --prefix frontend run build` succeeds and `frontend/out/index.html` exists.
    - TradeBar and ChatPanel receive refetch callbacks that re-fetch /api/portfolio and /api/watchlist.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the served terminal end-to-end</name>
  <files>frontend/out</files>
  <action>
    Build the static export and serve it via the existing backend with FRONTEND_DIST set, then present the running terminal for human visual + functional verification. Do NOT modify backend behavior — only set the FRONTEND_DIST env var to point at frontend/out. Wait for explicit approval before marking the phase done.
  </action>
  <what-built>The full FinAlly terminal assembled in app/page.tsx, built to a static export at frontend/out, and served by the existing FastAPI backend via FRONTEND_DIST.</what-built>
  <how-to-verify>
    1. Build the frontend: `npm --prefix frontend run build` (produces frontend/out).
    2. Start the backend pointed at the export (from repo root): `FRONTEND_DIST=frontend/out LLM_MOCK=true uv run --project backend --extra dev uvicorn app.main:app --app-dir backend --port 8000` (or the project's standard run command with FRONTEND_DIST set to the absolute path of frontend/out).
    3. Confirm the app is served: `curl -s http://localhost:8000/ | grep -qi finally` returns success, and `curl -s http://localhost:8000/api/health` returns `{"status":"ok"}`.
    4. Open http://localhost:8000 in a browser and visually verify: dark Bloomberg-style layout with the accent (#ecad0a) / blue (#209dd7) / purple (#753991) scheme; the header shows total value + cash + a connection dot that goes green; watchlist prices flash green/red and sparklines fill in progressively; clicking a ticker updates the main chart; the positions table, portfolio heatmap, and P&L chart render; the trade bar executes a buy instantly (no confirmation) and the portfolio updates; the collapsible chat panel sends a message (LLM_MOCK) and shows the loading indicator + any inline action confirmations.
  </how-to-verify>
  <verify>
    <human-check>Human confirms the terminal renders in the locked dark scheme and the key flows (live prices/flash/sparklines, ticker select → chart, positions/heatmap/P&L, instant trade, chat with loading + inline confirmations) work.</human-check>
  </verify>
  <done>Human approves the served terminal; all five phase success criteria are visually confirmed.</done>
  <resume-signal>Type "approved" once the terminal renders and the flows work, or describe what is broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static export → FastAPI FRONTEND_DIST | The built out/ directory is served at / by the backend |
| browser client → same-origin /api/* | Aggregate of all component data flows on one page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Information Disclosure | static export contents | low | mitigate | No secrets are embedded in the client bundle; all sensitive config (API keys) stays server-side in the backend env |
| T-04-12 | Denial of Service | on-mount + refetch fan-out | low | accept | Re-fetches are user-triggered (trade/chat/watchlist change) and lightweight; SSE is the only long-lived connection, already bounded by 04-01 |
</threat_model>

<verification>
- `npm --prefix frontend run build` produces `frontend/out/index.html`.
- Backend with `FRONTEND_DIST=frontend/out` serves the terminal at `/` and `/api/health` returns ok (human-verify checkpoint).
</verification>

<success_criteria>
- One dark, data-dense terminal page assembles all eight components in the locked scheme.
- Ticker selection drives the main chart; trades and chat actions refresh the portfolio/watchlist instantly.
- The static export builds and is served correctly by the existing backend.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-terminal-ui/04-06-SUMMARY.md` when done.
</output>
