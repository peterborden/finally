---
phase: 03-ai-chat-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
autonomous: false
requirements: [CHAT-02]
user_setup:
  - service: openrouter
    why: "Live (non-mock) chat calls the LLM via OpenRouter (Cerebras). Tests use LLM_MOCK=true and never need it."
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard -> Keys. Already present in the project-root .env per planning/PLAN.md §5 (gitignored)."

must_haves:
  truths:
    - "litellm is importable inside the backend uv environment"
    - "pydantic is declared as an explicit dependency"
  artifacts:
    - "backend/pyproject.toml lists litellm and pydantic under [project].dependencies"
    - "backend/uv.lock resolves litellm"
  key_links:
    - "litellm import path used later by app/llm.py (CHAT-02)"
---

<objective>
Provision the LiteLLM dependency that the Phase 3 LLM call requires, gated by a supply-chain legitimacy checkpoint.

Purpose: `app/llm.py` (plan 03-02) calls `litellm.completion` per the `cerebras-inference` skill; that import must resolve in the uv environment before the wrapper can be built or tested. This plan is isolated so the human-verify supply-chain gate does not block downstream implementation flow.
Output: `litellm` and `pydantic` declared in `backend/pyproject.toml`, resolved into `backend/uv.lock`, importable via `uv run`.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/cerebras/SKILL.md
@backend/pyproject.toml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify litellm + pydantic package legitimacy (supply-chain gate)</name>
  <files>backend/pyproject.toml</files>
  <action>
    STOP before Task 2 installs anything. No RESEARCH.md Package Legitimacy Audit exists for this
    phase (has_research=false), so per the planner fallback policy both installs are treated as
    [ASSUMED] and require human verification first. Present the two package pages to the human and
    wait for explicit approval before any install runs.
  </action>
  <what-built>
    About to install: litellm (new runtime dep, mandated by the checked-in
    .claude/skills/cerebras/SKILL.md) and pydantic (already used transitively via FastAPI; being made
    explicit per the skill).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/litellm/ — confirm it is the real BerriAI LiteLLM package (maintained, high download count, links to github.com/BerriAI/litellm).
    2. Open https://pypi.org/project/pydantic/ — confirm it is the canonical Pydantic package (pydantic/pydantic, current 2.x).
    3. Confirm neither name is a typo-squat of the intended package.
  </how-to-verify>
  <verify>
    <human-check>Human confirms both PyPI pages are the legitimate, canonical packages (not typo-squats).</human-check>
  </verify>
  <done>Human approves both packages as legitimate; install is authorized.</done>
  <resume-signal>Type "approved" to authorize the install, or describe the concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add litellm + pydantic to pyproject.toml and sync (CHAT-02)</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <action>
    Add `litellm>=1.0.0` and `pydantic>=2.0.0` to the `[project].dependencies` array in
    backend/pyproject.toml (runtime deps — the chat endpoint is production code, not test-only).
    Keep the existing entries and formatting; do not touch the `[project.optional-dependencies].dev`
    list. Then resolve and install with `uv sync --extra dev` run from the backend directory so
    uv.lock is regenerated. Do not add any other packages. Do not create app/llm.py here — that is
    plan 03-02. This enables CHAT-02 (the LiteLLM -> OpenRouter -> Cerebras call).
  </action>
  <verify>
    <automated>cd backend && grep -q '"litellm' pyproject.toml && grep -q '"pydantic' pyproject.toml && uv run python -c "import litellm, pydantic; print('ok')"</automated>
  </verify>
  <done>pyproject.toml declares litellm and pydantic; `uv run python -c "import litellm"` succeeds; uv.lock updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry -> build env | third-party code (litellm + its transitive deps) enters the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | pip/uv installs (litellm, pydantic) | high | mitigate | Blocking human-verify checkpoint (Task 1) confirms both packages on pypi.org before install; pinned via uv.lock |
</threat_model>

<verification>
- `uv run python -c "import litellm, pydantic"` exits 0 from backend/.
- pyproject.toml diff adds exactly litellm and pydantic to runtime dependencies.
</verification>

<success_criteria>
litellm and pydantic are explicit runtime dependencies, resolved in uv.lock, and importable in the backend uv environment.
</success_criteria>

<output>
Create `.planning/phases/03-ai-chat-assistant/03-01-SUMMARY.md` when done
</output>
