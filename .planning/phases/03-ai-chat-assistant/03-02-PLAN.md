---
phase: 03-ai-chat-assistant
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/llm.py
  - backend/tests/test_llm.py
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-07]

must_haves:
  truths:
    - "A structured Pydantic response schema parses the LLM JSON into message + trades + watchlist_changes"
    - "With LLM_MOCK=true the wrapper returns a deterministic response and makes no network call"
    - "With LLM_MOCK unset the wrapper calls litellm.completion using the cerebras skill's model/provider/response_format"
    - "The prompt is built from a system message + portfolio-context text + recent history + the new user message"
  artifacts:
    - "backend/app/llm.py"
    - "backend/tests/test_llm.py"
  key_links:
    - "generate_chat_response() is the single entry point chat.py (plan 03-03) calls"
    - "ChatResponse / TradeIntent / WatchlistChange types consumed by chat.py auto-execution"
---

<objective>
Build the LLM client wrapper `app/llm.py`: the structured-output schema, the prompt builder, the real Cerebras call (per the `cerebras-inference` skill), and the deterministic `LLM_MOCK` path — all behind one function so the HTTP layer never sees LiteLLM directly.

Purpose: Isolate the LLM concern (schema + prompt + call + mock switch) from the HTTP/DB/auto-execution concern (plan 03-03), keeping each within budget and making the mock switch a single clean boundary.
Output: `app/llm.py` exposing `ChatResponse`, `TradeIntent`, `WatchlistChange`, `build_messages`, `is_mock_enabled`, `generate_chat_response`; unit tests that never touch the network.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/cerebras/SKILL.md
@planning/PLAN.md
@backend/app/trade_engine.py
@backend/tests/test_portfolio.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement app/llm.py — schema, prompt builder, Cerebras call, mock (CHAT-02, CHAT-03, CHAT-07)</name>
  <files>backend/app/llm.py</files>
  <behavior>
    - ChatResponse.model_validate_json on `{"message":"hi"}` yields message="hi", trades=[], watchlist_changes=[] (both optional lists default empty).
    - ChatResponse parses nested trades [{ticker,side,quantity}] into TradeIntent and watchlist_changes [{ticker,action}] into WatchlistChange.
    - is_mock_enabled() is True for LLM_MOCK in {"true","TRUE","True"}, False when unset or "false".
    - generate_chat_response(context, history, "analyse [[buy:AAPL:10]]") in mock mode returns a ChatResponse with exactly one TradeIntent(ticker="AAPL", side="buy", quantity=10.0) and makes no litellm call.
    - mock message with "[[watch-add:PYPL]]" yields one WatchlistChange(ticker="PYPL", action="add"); "[[watch-remove:AAPL]]" yields action="remove".
    - mock message with no directive returns a non-empty message and empty trades/watchlist_changes.
    - non-mock generate_chat_response calls litellm.completion once with model=MODEL, response_format=ChatResponse, extra_body=EXTRA_BODY, and reasoning_effort set; returns the parsed ChatResponse from choices[0].message.content.
    - build_messages(context, history, user_msg) returns a list whose first element is the system role (containing the portfolio-context text), followed by the history messages in order, ending with the user message.
  </behavior>
  <action>
    Create backend/app/llm.py with `from __future__ import annotations`, a module docstring, and
    `logger = logging.getLogger(__name__)`. Follow the cerebras-inference skill EXACTLY for the real
    call: module constants MODEL = "openrouter/openai/gpt-oss-120b" and
    EXTRA_BODY = {"provider": {"order": ["cerebras"]}}; import `completion` from litellm lazily
    inside the real branch (so importing the module and running mock tests never require a network
    stack or key).

    Pydantic models (all with full type hints, Python 3.10+ union syntax):
    - TradeIntent: ticker: str, side: Literal["buy", "sell"], quantity: float.
    - WatchlistChange: ticker: str, action: Literal["add", "remove"].
    - ChatResponse: message: str (required), trades: list[TradeIntent] defaulting to an empty list,
      watchlist_changes: list[WatchlistChange] defaulting to an empty list. This is the structured
      output schema from planning/PLAN.md §9.

    SYSTEM_PROMPT constant: prompt the model as "FinAlly, an AI trading assistant" per PLAN.md §9
    System Prompt Guidance — analyze portfolio composition / risk concentration / P&L, suggest trades
    with reasoning, execute trades when the user asks or agrees, manage the watchlist, be concise and
    data-driven, and always return valid structured JSON matching the schema.

    build_messages(portfolio_context: str, history: list[dict], user_message: str) -> list[dict]:
    returns [{"role": "system", "content": SYSTEM_PROMPT + "\n\n" + portfolio_context}, *history,
    {"role": "user", "content": user_message}]. history items are already {"role","content"} dicts.

    is_mock_enabled() -> bool: True when os.environ.get("LLM_MOCK", "").strip().lower() == "true".

    generate_chat_response(portfolio_context: str, history: list[dict], user_message: str)
    -> ChatResponse:
      - if is_mock_enabled(): return _mock_response(user_message, portfolio_context).
      - else: messages = build_messages(...); call completion(model=MODEL, messages=messages,
        response_format=ChatResponse, reasoning_effort="low", extra_body=EXTRA_BODY); return
        ChatResponse.model_validate_json(response.choices[0].message.content).

    _mock_response(user_message, portfolio_context) -> ChatResponse: deterministic, no I/O. Scan
    user_message for directive tokens (documented, stable mock contract so tests and the Phase 5
    Playwright suite can drive actions without a key):
    `[[buy:TICKER:QTY]]` and `[[sell:TICKER:QTY]]` -> a TradeIntent; `[[watch-add:TICKER]]` and
    `[[watch-remove:TICKER]]` -> a WatchlistChange. Ticker is upper-cased; QTY parsed as float. Build
    a plain-English echo message summarizing the parsed actions (and, when none, a short deterministic
    acknowledgement that references that a portfolio context was provided). Document this token grammar
    in the module docstring.

    Add `__all__` listing the public API. Keep everything Ruff-clean at 100 cols. Do NOT import
    fastapi, sqlite3, or app.portfolio here — llm.py is transport-only and must stay unit-testable in
    isolation.
  </action>
  <verify>
    <automated>cd backend && uv run ruff check app/llm.py && uv run python -c "from app.llm import ChatResponse, generate_chat_response, build_messages, is_mock_enabled; print(ChatResponse.model_validate_json('{\"message\":\"hi\"}').trades)"</automated>
  </verify>
  <done>app/llm.py imports cleanly, passes Ruff, exposes the public API; ChatResponse parses with empty default action lists.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for app/llm.py — mock path + patched real path (CHAT-02, CHAT-07)</name>
  <files>backend/tests/test_llm.py</files>
  <action>
    Create backend/tests/test_llm.py (no TestClient needed — pure function tests). Use monkeypatch for
    LLM_MOCK and to patch litellm. Cover the behaviors listed in Task 1:
    - Schema: ChatResponse.model_validate_json for a message-only payload (empty trades /
      watchlist_changes) and for a payload with trades + watchlist_changes.
    - is_mock_enabled true/false across LLM_MOCK values (set via monkeypatch.setenv / delenv).
    - Mock path (monkeypatch.setenv("LLM_MOCK","true")): a message with `[[buy:AAPL:10]]` yields one
      buy TradeIntent; `[[watch-add:PYPL]]` yields one add WatchlistChange; a plain message yields a
      non-empty message and no actions. Assert no network by also patching litellm.completion to raise
      if called.
    - Real path (LLM_MOCK unset via monkeypatch.delenv(..., raising=False)): patch
      `app.llm.completion` (or monkeypatch litellm.completion) with a stub returning an object whose
      choices[0].message.content is a valid ChatResponse JSON string; assert generate_chat_response
      returns the parsed object AND assert the stub was called with model == the module MODEL constant,
      response_format == ChatResponse, and extra_body == the module EXTRA_BODY constant. The stub must
      make the test provably offline — never allow a real completion() call.
    - build_messages ordering: system first (contains the context text), history preserved, user last.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/test_llm.py -x -q</automated>
  </verify>
  <done>All test_llm.py tests pass; no test performs real network I/O (real-path test asserts the patched stub was invoked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> LiteLLM | tests must never reach the real OpenRouter/Cerebras endpoint |
| LLM output -> typed schema | untrusted model JSON is coerced through the ChatResponse Pydantic model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | OPENROUTER_API_KEY in live calls | medium | mitigate | Key read only from env inside litellm; never logged; tests run under LLM_MOCK or patched completion |
| T-03-02 | Tampering | malformed / unexpected LLM JSON | medium | mitigate | ChatResponse.model_validate_json rejects non-conforming payloads before any downstream use |
| T-03-03 | Spoofing | tests accidentally hitting the network | high | mitigate | Real-path test patches completion and asserts the stub was called; mock-path test makes completion raise if invoked |
</threat_model>

<verification>
- `uv run --extra dev pytest tests/test_llm.py -q` passes.
- `uv run ruff check app/llm.py tests/test_llm.py` clean.
</verification>

<success_criteria>
generate_chat_response returns a validated ChatResponse in both mock (deterministic, offline) and real (litellm.completion, patched in tests) modes, driven by LLM_MOCK; the prompt includes portfolio context + history + user message.
</success_criteria>

<output>
Create `.planning/phases/03-ai-chat-assistant/03-02-SUMMARY.md` when done
</output>
