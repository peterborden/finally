---
phase: 02-watchlist-trading-apis
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - backend/app/snapshots.py
  - backend/app/main.py
  - backend/tests/test_snapshots.py
autonomous: true
requirements: [PORT-06]

must_haves:
  truths:
    - "A background asyncio task records a portfolio_snapshots row on a fixed interval (default 30s) for the lifetime of the app (PORT-06, periodic half)"
    - "The snapshot interval is configurable so tests do not have to wait 30 seconds"
    - "The task is started in the app lifespan and cancelled/cleaned up on shutdown, matching the market source task pattern"
  artifacts:
    - backend/app/snapshots.py
    - backend/tests/test_snapshots.py
  key_links:
    - "the periodic task reuses portfolio.compute_and_record_snapshot(conn, price_cache) so periodic and post-trade snapshots share one code path"
    - "the task is started and cancelled inside app.main._build_lifespan, storing its handle on app.state"
---

<objective>
Add the periodic half of PORT-06: a background asyncio task, started in the app lifespan, that records a portfolio value snapshot every 30 seconds (interval configurable for tests) using the shared snapshot helper from plan 02-03, with proper cancel/cleanup on shutdown.

Purpose: Completes PORT-06 (portfolio value snapshotted every 30s AND immediately after each trade — the immediate half was done in plan 02-03). This feeds the Phase 4 P&L-over-time chart.
Output: `backend/app/snapshots.py` (the task runner), lifespan wiring in `app/main.py`, and a test that drives the task at a short interval.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-watchlist-trading-apis/02-CONTEXT.md

# Depends on:
@backend/app/portfolio.py   # created by plan 02-03 — compute_and_record_snapshot(conn, price_cache)
@backend/app/main.py        # lifespan closure — plans 02-01/02-03 registered routers; this adds a background task
@backend/app/db/connection.py
@backend/app/market/simulator.py   # reference for the asyncio task start/stop + cancel/await cleanup pattern
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Background snapshot task runner</name>
  <files>backend/app/snapshots.py</files>
  <read_first>
    - backend/app/portfolio.py — compute_and_record_snapshot(conn, price_cache); remember to open a fresh get_connection per tick and commit/close it (the periodic task is not inside an HTTP request).
    - backend/app/market/simulator.py — the asyncio.create_task(..., name=...) loop with try/finally cancel/await cleanup and the "log exception, continue on next interval" pattern; mirror it.
    - backend/app/db/connection.py — get_connection(get_db_path()).
  </read_first>
  <action>
    Create backend/app/snapshots.py exposing a SnapshotRecorder (or create_snapshot_task) that runs an async loop: every `interval` seconds, open a connection with get_connection(get_db_path()), call portfolio.compute_and_record_snapshot(conn, price_cache), commit, close. Conventions: `from __future__ import annotations`, type hints, module docstring + logger.

    Resolve the interval from an env var (e.g. SNAPSHOT_INTERVAL_SECONDS) defaulting to 30.0 so tests can set it to a small value; validate it is positive and fall back to the default with a warning otherwise. Expose start(price_cache) that creates the asyncio task with a descriptive name (e.g. name="portfolio-snapshotter") and stop() that cancels the task and awaits it, swallowing CancelledError — mirror SimulatorDataSource's start/stop lifecycle so shutdown is clean. Wrap the per-tick body in try/except so a transient DB/valuation error is logged and the loop continues on the next interval (do not let one bad tick kill the task).

    Read prices only from the injected price_cache (do not construct a data source).
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev ruff check app/snapshots.py && uv run --extra dev python -c "import app.snapshots; print('ok')"</automated>
  </verify>
  <done>snapshots.py imports cleanly; exposes start/stop lifecycle; interval read from env with a positive-default fallback; per-tick errors are caught; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the task into the lifespan + test at a short interval</name>
  <files>backend/app/main.py, backend/tests/test_snapshots.py</files>
  <read_first>
    - backend/app/main.py — _build_lifespan(cache): the closure already starts/stops the market source and sets app.state.market_source. Add the snapshot task start after the source starts and stop it in the finally block, guarding cleanup like the market source (try/except around stop).
    - backend/tests/test_main.py — db_path/client fixtures; set SNAPSHOT_INTERVAL_SECONDS to a small value (e.g. 0.05) via monkeypatch.setenv before create_app() so the test does not wait.
  </read_first>
  <action>
    In backend/app/main.py: inside _build_lifespan, after the market source has started, create the snapshot recorder (from .snapshots) bound to the same cache, start it, and store the handle on app.state (e.g. app.state.snapshot_task). In the finally block, stop/cancel it before or alongside stopping the market source, each guarded so one failing cleanup does not prevent the other. Initialize app.state.snapshot_task to None at the top like app.state.market_source. Keep the "log and continue if it fails to start" resilience so a snapshot failure never blocks the API/static/DB parts of the app from serving.

    Create backend/tests/test_snapshots.py: monkeypatch FINALLY_DB_PATH (tmp) and SNAPSHOT_INTERVAL_SECONDS to a small value (e.g. 0.05) BEFORE create_app(); seed a price into app.state.price_cache; make one request so the DB is initialized; then await a short asyncio.sleep (a few intervals) or poll GET /api/portfolio/history until it returns snapshot rows, asserting that periodic snapshots accumulate (count grows over time) without any trade occurring — proving the periodic path (PORT-06). Also assert that after the client context exits (lifespan shutdown) no exception is raised, i.e. the task cancels cleanly. Keep the test fast (sub-second) by using the small interval.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/test_snapshots.py -v && uv run --extra dev ruff check app/main.py tests/test_snapshots.py</automated>
  </verify>
  <done>The lifespan starts and cleanly cancels the snapshot task; with a small interval the test observes periodic snapshot rows accumulating without any trade; shutdown raises nothing; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background task → DB | Long-lived loop writing snapshot rows independent of any request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Denial of Service | per-tick DB connection | medium | mitigate | Open/commit/close a fresh connection each tick; catch and log per-tick errors so a bad tick does not kill the loop or leak connections |
| T-02-11 | Denial of Service | task cancellation on shutdown | medium | mitigate | stop() cancels and awaits the task swallowing CancelledError; lifespan finally guards it so shutdown never hangs |
| T-02-12 | Tampering | SNAPSHOT_INTERVAL_SECONDS env | low | mitigate | Parse defensively; non-positive/invalid values fall back to the 30s default with a warning |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest tests/test_snapshots.py -v` — passes fast (small interval)
- `cd backend && uv run --extra dev pytest -v` — full suite green (watchlist + trade_engine + portfolio + snapshots + Phase 1)
- `cd backend && uv run --extra dev ruff check app/ tests/` — clean
</verification>

<success_criteria>
Portfolio value is snapshotted every 30 seconds by a background task started in the lifespan (interval configurable for tests) and cancelled cleanly on shutdown, completing PORT-06 alongside the per-trade snapshot from plan 02-03. (Phase 2 success criterion 5, periodic half.)
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-trading-apis/02-04-SUMMARY.md` when done.
</output>
