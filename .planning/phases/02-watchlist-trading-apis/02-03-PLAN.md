---
phase: 02-watchlist-trading-apis
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - backend/app/portfolio.py
  - backend/app/main.py
  - backend/tests/test_portfolio.py
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03, PORT-05, PORT-06]

must_haves:
  truths:
    - "GET /api/portfolio returns cash_balance, positions[] (each with quantity, avg_cost, current_price, unrealized_pnl, pct_change), total_value = cash + sum(qty*current_price), and total unrealized P&L (PORT-01)"
    - "POST /api/portfolio/trade fills a market order instantly at the current cached price, updating cash + positions and appending a trades row (PORT-02, PORT-04)"
    - "A buy with insufficient cash or a sell with insufficient shares returns HTTP 400 with a clear message and leaves cash/positions unchanged (PORT-03)"
    - "Every successful trade writes an immediate portfolio_snapshots row (PORT-06, immediate half)"
    - "GET /api/portfolio/history returns portfolio_snapshots ordered by recorded_at (PORT-05)"
  artifacts:
    - backend/app/portfolio.py
    - backend/tests/test_portfolio.py
  key_links:
    - "portfolio valuation reads current prices from request.app.state.price_cache.get_price(ticker)"
    - "trade execution applies app.trade_engine.apply_trade, persists the result, and calls the shared snapshot helper in one DB transaction"
    - "snapshot helper compute_and_record_snapshot(conn, price_cache) is exported for reuse by the plan 02-04 background task"
---

<objective>
Deliver the portfolio and trading REST API: read portfolio state (positions + cash + total value + unrealized P&L), execute an instant market order that updates cash/positions and logs the trade, reject invalid trades with a clear 400, snapshot portfolio value immediately after each trade, and return the snapshot history. Trade math comes from the pure `app.trade_engine` (plan 02-02); live prices come from the shared `PriceCache`.

Purpose: Satisfies PORT-01, PORT-02, PORT-03, PORT-05, and the immediate-snapshot half of PORT-06 — the trading heart of Phase 2. Phase 3's AI chat reuses this same trade path.
Output: `backend/app/portfolio.py` (service functions + snapshot helper + router factory + Pydantic models), registration in `app/main.py`, and TestClient tests.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-watchlist-trading-apis/02-CONTEXT.md

# Depends on:
@backend/app/trade_engine.py       # created by plan 02-02 — apply_trade / TradeResult / TradeError
@backend/app/main.py               # register router (plan 02-01 already added the watchlist router here)
@backend/app/db/connection.py
@backend/app/market/__init__.py
@backend/tests/test_main.py
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portfolio service — read/valuation, trade persistence, snapshot helper</name>
  <files>backend/app/portfolio.py</files>
  <read_first>
    - backend/app/trade_engine.py — apply_trade signature and the TradeResult fields (new_cash, new_quantity, new_avg_cost, remove_position, filled_quantity, filled_price) and the rejection type/message.
    - backend/app/db/connection.py — get_connection(get_db_path()) yields a sqlite3.Row connection; commit explicitly; parameterize all SQL.
    - backend/CLAUDE.md — PriceCache.get_price(ticker) -> float | None and PriceUpdate fields.
    - .planning/phases/02-watchlist-trading-apis/02-CONTEXT.md — unrealized P&L = (current_price - avg_cost)*quantity; pct_change = current_price/avg_cost - 1; total_value = cash + sum(qty*current_price).
  </read_first>
  <action>
    Create backend/app/portfolio.py with the service layer (pure-ish helpers that take a sqlite3 connection and the price cache; no FastAPI in these functions so they are unit-testable and reusable by the plan 02-04 background task). Conventions: `from __future__ import annotations`, type hints, `str | None`, module docstring + logger, parameterized SQL, "default" user_id constant.

    Functions:
    - read_cash(conn) -> float: SELECT cash_balance FROM users_profile WHERE id='default'.
    - read_positions(conn) -> list of position rows (ticker, quantity, avg_cost).
    - build_portfolio(conn, price_cache) -> a structure with cash_balance, positions (each: ticker, quantity, avg_cost, current_price, unrealized_pnl, pct_change), total_value, total_unrealized_pnl. current_price comes from price_cache.get_price(ticker); when the cache has no price, fall back to avg_cost for valuation (so total_value stays finite) and mark current_price accordingly — pick a consistent rule and document it in the docstring. unrealized_pnl and pct_change follow the CONTEXT formulas; guard avg_cost==0 for pct_change.
    - compute_and_record_snapshot(conn, price_cache) -> float: compute total_value via build_portfolio and INSERT a portfolio_snapshots row (id=uuid4().hex, user_id, total_value, recorded_at=UTC isoformat); return the recorded total_value. This is the shared helper reused by the immediate post-trade snapshot AND the plan 02-04 background task. Do not commit inside this helper if it is called within a larger transaction — accept the connection and let the caller commit (document this).
    - execute_trade(conn, price_cache, ticker, side, quantity) -> the executed trade + resulting portfolio: read current price from price_cache.get_price(ticker) (raise a rejection if no price is available for the ticker), read current cash and the existing position (quantity 0 / avg_cost 0 if none), call trade_engine.apply_trade, and on success: UPDATE users_profile.cash_balance; UPSERT the positions row (INSERT OR REPLACE / ON CONFLICT on UNIQUE(user_id,ticker)) or DELETE the row when remove_position is True; INSERT a trades row (id, user_id, ticker, side, quantity=filled_quantity, price=filled_price, executed_at); call compute_and_record_snapshot; commit once. On a trade_engine rejection, do not write anything and re-raise/return the rejection so the router maps it to 400. Wrap the writes so a failure rolls back (single commit at the end; on exception let the connection close without commit).

    Keep the market-data anti-pattern in mind: read prices only from the injected price_cache, never construct or call a data source here.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev ruff check app/portfolio.py && uv run --extra dev python -c "import app.portfolio as p; assert all(hasattr(p, n) for n in ['build_portfolio','execute_trade','compute_and_record_snapshot']); print('ok')"</automated>
  </verify>
  <done>portfolio.py imports cleanly; build_portfolio/execute_trade/compute_and_record_snapshot exist; all SQL parameterized; prices read only from the injected cache; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio router (GET portfolio, POST trade, GET history) + register in main</name>
  <files>backend/app/portfolio.py, backend/app/main.py</files>
  <read_first>
    - backend/app/main.py — plan 02-01 added `app.include_router(create_watchlist_router())` before the static mount; add the portfolio router the same way, before the StaticFiles mount.
    - backend/app/trade_engine.py — the rejection type to catch and translate into HTTPException(status_code=400, detail=message).
  </read_first>
  <action>
    In backend/app/portfolio.py add create_portfolio_router() -> APIRouter with Pydantic request/response models and these endpoints (obtain conn via get_connection(get_db_path()) with a finally-close; obtain the cache via request.app.state.price_cache):

    - GET /api/portfolio (PORT-01): return build_portfolio(...) serialized — cash_balance, positions[] (ticker, quantity, avg_cost, current_price, unrealized_pnl, pct_change), total_value, total_unrealized_pnl.
    - POST /api/portfolio/trade (PORT-02, PORT-03, PORT-04, PORT-06): request model TradeRequest with ticker: str (normalized upper-case via validator), side: Literal["buy","sell"], quantity: float (must be > 0 — Pydantic field constraint). Call execute_trade(...). On a trade_engine rejection (insufficient cash/shares/invalid), raise HTTPException(400, detail=<message>) — the DB must be unchanged. On success return the executed trade plus the refreshed portfolio (so the caller sees new cash/positions in one round-trip).
    - GET /api/portfolio/history (PORT-05): SELECT id, total_value, recorded_at FROM portfolio_snapshots WHERE user_id=? ORDER BY recorded_at; return the list.

    In backend/app/main.py: import create_portfolio_router from .portfolio and add `app.include_router(create_portfolio_router())` next to the watchlist/SSE router registrations, before the static mount. Do not touch the lifespan in this plan (the background snapshot task is plan 02-04).
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev ruff check app/portfolio.py app/main.py && uv run --extra dev python -c "from app.portfolio import create_portfolio_router; r=create_portfolio_router(); print(sorted({rt.path for rt in r.routes}))"</automated>
  </verify>
  <done>Router exposes /api/portfolio, /api/portfolio/trade, /api/portfolio/history; registered in create_app before the static mount; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3: TestClient tests for portfolio + trading (incl. error cases)</name>
  <files>backend/tests/test_portfolio.py</files>
  <read_first>
    - backend/tests/test_main.py and backend/tests/test_watchlist.py — reuse the db_path (FINALLY_DB_PATH monkeypatch) and client (`with TestClient(app)`) fixtures, and the pattern of seeding a deterministic price via app.state.price_cache.update(ticker, price).
  </read_first>
  <action>
    Create backend/tests/test_portfolio.py with FastAPI TestClient. In each trade test, seed a deterministic fill price into the cache with app.state.price_cache.update("AAPL", 100.0) so trades have a stable fill price independent of the simulator.

    Cases:
    - GET /api/portfolio on a fresh DB: cash_balance == 10000.0, positions == [], total_value == 10000.0, total_unrealized_pnl == 0.
    - Buy 10 AAPL @ 100: 200; cash == 9000.0; a subsequent GET shows one AAPL position qty 10, avg_cost 100; total_value == 10000 (100*10 + 9000) at the seeded price; a trades row exists.
    - Buy again 10 AAPL @ 200 (re-seed cache to 200): avg_cost becomes 150 (weighted); qty 20 (PORT-04 through the endpoint).
    - Fractional buy (e.g. 2.5 shares) updates cash and position with exact math.
    - Sell all shares: position disappears from GET /api/portfolio; cash increased.
    - Insufficient cash buy (qty*price > cash): 400, detail message present, and a follow-up GET shows cash/positions UNCHANGED (PORT-03).
    - Insufficient shares sell (sell more than held, or sell with no position): 400, portfolio unchanged (PORT-03).
    - quantity <= 0 in the request body: 422 (Pydantic) or 400.
    - Immediate snapshot (PORT-06): after a successful trade, GET /api/portfolio/history returns at least one snapshot row whose total_value matches the post-trade total.
    - GET /api/portfolio/history ordering: after two trades, snapshots are returned ordered by recorded_at.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/test_portfolio.py -v && uv run --extra dev ruff check tests/test_portfolio.py</automated>
  </verify>
  <done>All portfolio/trade tests pass including the two rejection cases proving the portfolio is unchanged, the weighted-average-cost case through the endpoint, and the immediate-snapshot case. Ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/portfolio/trade | Untrusted ticker/side/quantity in the trade body |
| price_cache → valuation | Cached price may be missing for a requested ticker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | POST /api/portfolio/trade | high | mitigate | apply_trade validation (plan 02-02) rejects insufficient cash/shares and non-positive qty before any DB write; endpoint maps rejection to 400 |
| T-02-07 | Tampering | trade DB writes | high | mitigate | Single transaction (cash update + position upsert/delete + trade insert + snapshot) with one commit; on exception no commit -> no partial state; all SQL parameterized |
| T-02-08 | Denial of Service | trade quantity field | medium | mitigate | Pydantic constrains quantity > 0; missing cache price rejected rather than valued as NaN/inf |
| T-02-09 | Tampering | side field | medium | mitigate | side constrained to Literal["buy","sell"] by Pydantic |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest tests/test_portfolio.py -v` — all pass
- `cd backend && uv run --extra dev ruff check app/portfolio.py app/main.py tests/test_portfolio.py` — clean
- The two rejection tests assert an unchanged portfolio after a 400 (PORT-03 evidence).
</verification>

<success_criteria>
GET /api/portfolio returns positions/cash/total value/unrealized P&L; POST /api/portfolio/trade fills instantly at the cached price with correct average-cost math and fractional shares; insufficient cash/shares are rejected with a clear 400 and no state change; each trade writes an immediate snapshot; GET /api/portfolio/history returns snapshots. (Phase 2 success criteria 2, 3, 4, and the immediate half of 5.)
</success_criteria>

<output>
Create `.planning/phases/02-watchlist-trading-apis/02-03-SUMMARY.md` when done.
</output>
