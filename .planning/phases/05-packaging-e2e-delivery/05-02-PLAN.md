---
phase: 05-packaging-e2e-delivery
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/start_mac.sh
  - scripts/stop_mac.sh
  - scripts/start_windows.ps1
  - scripts/stop_windows.ps1
  - .env.example
  - README.md
  - docker-compose.yml
autonomous: true
requirements: [PKG-03]
user_setup: []

must_haves:
  truths:
    - "A user with Docker can launch the whole app with one command (scripts/start_mac.sh on macOS/Linux, scripts/start_windows.ps1 on Windows)"
    - "start scripts are idempotent: re-running builds only if needed, reuses the named volume, and never errors on a second run"
    - "stop scripts remove the running container but preserve the finally-data volume so data survives"
    - ".env.example documents OPENROUTER_API_KEY, MASSIVE_API_KEY, and LLM_MOCK without any real secret values"
  artifacts:
    - "scripts/start_mac.sh and scripts/stop_mac.sh (bash, idempotent)"
    - "scripts/start_windows.ps1 and scripts/stop_windows.ps1 (PowerShell, idempotent)"
    - ".env.example at repo root"
    - "README.md 'Run with Docker' section documenting the one-command launch"
    - "docker-compose.yml convenience wrapper at repo root"
  key_links:
    - "All scripts and the compose file use the same image name `finally`, container name `finally`, volume `finally-data`, port 8000, and --env-file .env"
    - "start scripts pass --env-file .env so OPENROUTER_API_KEY / LLM_MOCK reach the container"
---

<objective>
Give a user a genuine one-command launch: idempotent start/stop scripts for macOS/Linux and Windows, a committed .env.example, a convenience docker-compose.yml wrapper, and a README run section — all built on the `finally` image from Plan 01.

Purpose: PKG-03 (idempotent start/stop scripts for both platforms plus .env.example that let a user launch with one command).

Output: scripts/ (four files), .env.example, docker-compose.yml, and an updated README.
</objective>

<execution_context>
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/family/projects/peterai/finally2/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-e2e-delivery/05-CONTEXT.md

# The image, volume, and port these scripts wrap (from Plan 01)
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .env.example, docker-compose.yml, and the README run section</name>
  <files>.env.example, docker-compose.yml, README.md</files>
  <read_first>
    - planning/PLAN.md §5 (env var names + behavior) — do NOT read or copy the real .env
    - README.md (existing top-level doc; append a run section, do not rewrite it)
    - Dockerfile (image name/port/volume conventions to mirror)
  </read_first>
  <action>
    Create .env.example at the repo root with exactly the three documented variables and empty/default values plus a short comment on each (per PLAN.md §5): `OPENROUTER_API_KEY=` (required for real LLM chat), `MASSIVE_API_KEY=` (optional; blank → built-in simulator), and `LLM_MOCK=false` (set true for deterministic offline chat / tests). Never place a real key here — the real .env stays gitignored and untouched.

    Create docker-compose.yml at the repo root as a convenience wrapper: one service `finally` that builds from the repo-root Dockerfile (`build: .`), maps `8000:8000`, mounts the named volume `finally-data:/app/db`, reads `env_file: .env`, and sets `container_name: finally`. Declare the `finally-data` volume. This mirrors the manual `docker run -v finally-data:/app/db -p 8000:8000 --env-file .env finally` invocation. Keep it minimal — it is optional convenience, not the primary path.

    Append a `## Run with Docker` section to README.md documenting: (1) copy `.env.example` to `.env` and fill keys (or leave blank / set LLM_MOCK=true to run without keys); (2) the one-command launch via `scripts/start_mac.sh` (macOS/Linux) or `scripts/start_windows.ps1` (Windows); (3) the equivalent raw `docker run -v finally-data:/app/db -p 8000:8000 --env-file .env finally` and `docker compose up` commands; (4) the app URL http://localhost:8000; (5) that stopping via the stop scripts preserves the finally-data volume. Do not delete existing README content.
  </action>
  <verify>
    <automated>grep -q '^OPENROUTER_API_KEY=' .env.example && grep -q '^MASSIVE_API_KEY=' .env.example && grep -q '^LLM_MOCK=' .env.example && ! grep -qE 'OPENROUTER_API_KEY=.+' .env.example && grep -q 'finally-data:/app/db' docker-compose.yml && grep -qi 'Run with Docker' README.md && echo OK</automated>
  </verify>
  <done>.env.example lists the three vars with no real secret; docker-compose.yml wraps the image with the volume+port+env_file; README documents the one-command run.</done>
</task>

<task type="auto">
  <name>Task 2: Author idempotent macOS/Linux start and stop scripts</name>
  <files>scripts/start_mac.sh, scripts/stop_mac.sh</files>
  <read_first>
    - Dockerfile (image name `finally`, port 8000, volume mount /app/db)
    - .env.example (created in Task 1 — start script copies it to .env if .env is absent)
  </read_first>
  <action>
    Create scripts/start_mac.sh (bash, `set -euo pipefail`), idempotent:
    - Resolve the repo root relative to the script location so it runs from anywhere.
    - If .env does not exist, copy .env.example → .env and print a note that keys can be filled in (so a first-time user can launch immediately; with blank keys the simulator + real LLM-less path still boot, and LLM_MOCK can be set).
    - Build the image only if needed: build when `docker image inspect finally` fails OR when a `--build` argument is passed; otherwise skip the build and reuse the existing image. Print which path was taken.
    - If a container named `finally` is already running, print its URL and exit 0 (idempotent — do not error or double-run). If a stopped container named `finally` exists, remove it before starting a fresh one.
    - Run detached: `docker run -d --name finally -p 8000:8000 -v finally-data:/app/db --env-file .env finally`.
    - Print the access URL http://localhost:8000. Optionally open the browser on macOS via `open` guarded by a command-exists check (never fail if `open` is unavailable, e.g. on Linux).
    - Safe to re-run: a second invocation with the container already up must exit 0 without creating a duplicate.

    Create scripts/stop_mac.sh (bash, `set -euo pipefail`), idempotent:
    - Stop and remove the `finally` container if it exists (guard with existence checks so a second run on an already-stopped app exits 0, not error).
    - Explicitly DO NOT remove the `finally-data` volume — print a line confirming data is preserved.

    Make both scripts executable (chmod +x). Use only the image name/port/volume conventions from Plan 01.
  </action>
  <verify>
    <automated>bash -n scripts/start_mac.sh && bash -n scripts/stop_mac.sh && test -x scripts/start_mac.sh && test -x scripts/stop_mac.sh && grep -q 'finally-data:/app/db' scripts/start_mac.sh && grep -q -- '--env-file .env' scripts/start_mac.sh && grep -qE 'image inspect finally|--build' scripts/start_mac.sh && ! grep -q 'volume rm' scripts/stop_mac.sh && bash scripts/start_mac.sh && curl -fsS http://localhost:8000/api/health | grep -q ok && bash scripts/start_mac.sh && curl -fsS http://localhost:8000/api/health | grep -q ok && bash scripts/stop_mac.sh && bash scripts/stop_mac.sh && docker volume inspect finally-data >/dev/null && echo IDEMPOTENT_OK; docker rm -f finally >/dev/null 2>&1; true</automated>
  </verify>
  <done>start_mac.sh builds-if-needed, launches on 8000 with the finally-data volume and --env-file, is safe to run twice, and prints the URL; stop_mac.sh removes the container twice without error and keeps the volume.</done>
</task>

<task type="auto">
  <name>Task 3: Author idempotent Windows PowerShell start and stop scripts</name>
  <files>scripts/start_windows.ps1, scripts/stop_windows.ps1</files>
  <read_first>
    - scripts/start_mac.sh (mirror its logic/naming so both platforms behave identically)
    - Dockerfile (image name `finally`, port 8000, volume /app/db)
  </read_first>
  <action>
    Create scripts/start_windows.ps1 mirroring start_mac.sh behavior in PowerShell, idempotent:
    - `$ErrorActionPreference = 'Stop'`; resolve the repo root from `$PSScriptRoot`.
    - If .env is absent, copy .env.example → .env (Copy-Item) and print a note.
    - Build only if `docker image inspect finally` fails OR a `-Build` switch parameter is passed.
    - If a container named `finally` is running, print the URL and exit 0; if a stopped `finally` container exists, remove it first.
    - Run: `docker run -d --name finally -p 8000:8000 -v finally-data:/app/db --env-file .env finally`.
    - Print http://localhost:8000. Optionally open the browser via `Start-Process` guarded so it never throws.

    Create scripts/stop_windows.ps1 mirroring stop_mac.sh: stop+remove the `finally` container guarded by existence checks (idempotent), and explicitly do NOT remove the finally-data volume (print a confirmation that data persists).

    Use the same image/container/volume/port names as the macOS scripts so the two platforms are interchangeable.
  </action>
  <verify>
    <automated>test -f scripts/start_windows.ps1 && test -f scripts/stop_windows.ps1 && grep -q 'finally-data:/app/db' scripts/start_windows.ps1 && grep -q -- '--env-file .env' scripts/start_windows.ps1 && grep -qiE 'image inspect finally|-Build' scripts/start_windows.ps1 && ! grep -qi 'volume rm' scripts/stop_windows.ps1 && grep -qi 'ErrorActionPreference' scripts/start_windows.ps1 && { command -v pwsh >/dev/null 2>&1 && pwsh -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile('scripts/start_windows.ps1',[ref]$null,[ref]$null); $null = [System.Management.Automation.Language.Parser]::ParseFile('scripts/stop_windows.ps1',[ref]$null,[ref]$null)" || echo "pwsh unavailable — static checks only"; } && echo OK</automated>
  </verify>
  <done>Windows start/stop scripts mirror the macOS scripts (same image/volume/port/env-file), build-if-needed with a -Build switch, and preserve the volume on stop. PowerShell parses cleanly if pwsh is available; otherwise static grep checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → container | Real secrets are passed via --env-file; must never be committed or copied into the image |
| user shell → docker daemon | Scripts issue privileged docker commands on the user's behalf |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | .env.example / .env handling | high | mitigate | .env.example ships empty values only; scripts copy example→.env locally and never echo key values; real .env stays gitignored |
| T-05-05 | Denial of Service | Non-idempotent re-run | low | mitigate | Existence-guarded start/stop so repeated runs never crash or spawn duplicate containers |
| T-05-06 | Tampering | Accidental volume deletion | medium | mitigate | stop scripts explicitly never call `docker volume rm`; verified by a negative grep |
</threat_model>

<verification>
- .env.example contains only the three documented variables with empty/default values (no real secrets).
- start_mac.sh launches the app on http://localhost:8000, is safe to run twice, and stop_mac.sh is safe to run twice while preserving finally-data.
- Windows scripts mirror the macOS scripts and parse under PowerShell where available.
- README documents the one-command run and the volume-preserving stop.
</verification>

<success_criteria>
PKG-03: idempotent start/stop scripts for macOS/Linux and Windows plus a committed .env.example let a user launch the app with one command; stopping preserves data.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-e2e-delivery/05-02-SUMMARY.md` when done.
</output>