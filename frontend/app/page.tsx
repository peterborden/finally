'use client';

export default function Home() {
  return (
    <main className="min-h-screen bg-base">
      <header className="flex items-center border-b border-border-muted px-4 py-3">
        <h1 className="text-lg font-semibold text-accent">FinAlly</h1>
      </header>
      <div className="p-4 text-sm text-gray-400">
        Terminal panels mount here. (Placeholder shell — replaced by the full
        Bloomberg-style layout in plan 04-06.)
      </div>
    </main>
  );
}
